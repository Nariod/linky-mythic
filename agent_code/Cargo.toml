[workspace]
members = [
    "links/windows",
    "links/linux",
    "links/osx",
    "links/common",
]
resolver = "2"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true

[profile.release-shellcode]
inherits = "release"
panic = "abort"
